---
name: agent-worker-deploy
description: Deploy, update, roll back, or health-check an agent worker.
---

Deploy or verify an agent worker using the `agent-worker-deploy` subagent.

Follow the agent workflow: scope → inventory → preflight → deploy → verify → report.
Ask only for missing target environment, credentials, or success criteria.
